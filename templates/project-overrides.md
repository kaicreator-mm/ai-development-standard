# Project Overrides Template

Canonical project-adoption template:

`templates/project/.dev-standard/PROJECT_OVERRIDES.md`

Use the canonical file for new projects. This compatibility entry remains so older instructions that reference `templates/project-overrides.md` do not silently resolve to outdated v1.0 content.

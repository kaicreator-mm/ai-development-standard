# <Project Name>

<One paragraph: what the project is, who/what it serves, and the primary outcome.>

## Modules

- `<path>` — `<responsibility>`

## Quick Start

```bash
<bootstrap command>
<run command>
```

## Validation

```bash
<fast validation command>
```

See `.dev-standard/PROJECT_OVERRIDES.md` for the canonical project commands and required release gates.

## Documentation

Start at [`docs/README.md`](docs/README.md).

## Development Standard

This repository pins an immutable revision of `kaicreator-mm/ai-development-standard` in `.dev-standard/VERSION`. Do not silently follow the standard repository's latest `main`.
